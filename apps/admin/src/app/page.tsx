import { PrimaryButton } from "ui/buttons";

export default function Home() {
  return (
    <main>
      <p>
        This is using <code>packages/shared/ui</code> &darr;
      </p>
      <PrimaryButton>Shared Ui Button</PrimaryButton>
    </main>
  );
}
